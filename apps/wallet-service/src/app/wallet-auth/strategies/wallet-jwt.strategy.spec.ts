import { UnauthorizedException } from '@nestjs/common'; import { WALLET_JWT_IGNORE_EXPIRATION, WalletJwtStrategy } from './wallet-jwt.strategy';
describe('WalletJwtStrategy access-token validation',()=>{const previous=process.env.JWT_SECRET;beforeAll(()=>{process.env.JWT_SECRET='test-only-secret-that-is-not-a-production-credential';});afterAll(()=>{if(previous===undefined)delete process.env.JWT_SECRET;else process.env.JWT_SECRET=previous;});it('derives identity from JWT claims and rejects refresh or malformed payloads',()=>{const strategy=new WalletJwtStrategy();expect(strategy.validate({sub:'user-1',email:'user@example.test',role:'USER'})).toEqual({id:'user-1',email:'user@example.test',role:'USER'});expect(()=>strategy.validate({sub:'user-1',email:'user@example.test',role:'USER',type:'refresh'})).toThrow(UnauthorizedException);expect(()=>strategy.validate({sub:'',email:'',role:''})).toThrow(UnauthorizedException);});it('configures Passport to reject expired access tokens',()=>{expect(WALLET_JWT_IGNORE_EXPIRATION).toBe(false);});});
